{
  "name": "androidlog",
  "version": "1.0.0",
  "description": "Just a simple logger util that tries to mimick the way android.util.Log.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist/ && babel ./src/ --out-dir ./dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,/.gitignore --copy-files",
    "start": "npm run build && nodemon ./dist/start.js || true",
    "test": "jest"
  },
  "author": "Andreas Göransson (http://github.com/agoransson)",
  "license": "Apache-2.0",
  "dependencies": {
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.5",
    "@babel/core": "^7.7.5",
    "@babel/node": "^7.7.4",
    "@babel/preset-env": "^7.7.6",
    "@babel/register": "^7.7.4",
    "chai": "^4.2.0",
    "jest": "^24.9.0",
    "jest-mock-console": "^1.0.0",
    "nodemon": "^2.0.2"
  }
}
