{
  "name": "@agoransson/androidlog",
  "version": "1.0.3",
  "description": "Just a simple logger util that tries to mimick the way android.util.Log.",
  "main": "lib/Log",
  "scripts": {
    "build": "rm -rf lib/ && babel ./src/ --out-dir ./lib/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,/.gitignore --copy-files",
    "prepublish": "babel src --out-dir lib",
    "test": "jest"
  },
  "author": "Andreas Göransson (http://github.com/agoransson)",
  "license": "Apache-2.0",
  "dependencies": {
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.7.5",
    "@babel/core": "^7.7.5",
    "@babel/node": "^7.7.4",
    "@babel/preset-env": "^7.7.6",
    "@babel/register": "^7.7.4",
    "chai": "^4.2.0",
    "jest": "^29.3.1",
    "jest-mock-console": "^1.0.0"
  }
}
